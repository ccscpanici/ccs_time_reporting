# Work Code Display Update

Changes:
- Work code dropdowns still show full `code - description` when selecting.
- Once selected/collapsed, work code fields display only the code to save horizontal space.
- Read-only timesheet detail/report views now show only the work code code, not the description.
